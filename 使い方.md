# ろばみみ打刻の使い方

## 初回設定

1. 通常のChromeで [ろばみみ打刻](https://xxdaijoubuxx.github.io/robamimi-dakoku/) を開く。
2. 画面の案内に従ってGoogleで本人確認する。
3. 「この端末を本人確認済みにする」を実行する。
4. 「オフライン準備」が完了したことを確認する。
5. Chromeの「ホーム画面に追加」から、次の4入口を追加する。表示名は手動で設定する。
   - 起床
   - 就寝
   - メモ
   - 履歴

## 日常の記録

- 起床・就寝：ホーム画面の入口を1回押す。入力や確認は不要。
- メモ：入口を押した時点で時刻が保存される。本文は余裕があるときだけ入力する。
- 本文はEnterを押さなくても、入力が止まると自動保存される。
- 履歴は新しい記録から表示される。

起床・就寝の本文欄は初版では使わない。補足を残すときは、別途「メモ」を記録する。

## 状態の見方

- 「端末に保存済み」：スマートフォン内への保存が完了。
- 「同期済み」：Firebaseへの保存も完了。
- 「同期待ち」：端末内には保存済み。通信回復後に入口を開くと自動同期される。
- 「同期失敗」「再ログインが必要」「競合」：履歴または状態画面から内容を確認し、表示された再試行・再認証・解決を行う。

圏外で「同期を再試行」を押しても、記録は消えない。圏外である旨が表示されるので、通信回復後に再試行する。

## 修正・削除・復元

- 履歴から記録を開くと、日時・種類・メモ本文を修正できる。
- 削除は確認後に実行する。
- 削除した記録は「最近削除した記録」から30日以内に復元できる。

## CSVと診断情報

- 履歴の「CSVを保存」で、削除済みを除く端末内の全記録を最新順で保存する。
- 障害時は「状態を確認」から、本文や認証情報を含まない診断情報を保存できる。

## 更新

新版があるときは「更新する」が表示される。押すと新版へ切り替わる。更新で端末内記録は削除されない。

## 3日間の試用

起床・就寝・必要なメモを普段どおり記録する。次をメモしておく。

- 記録の消失や重複がないか
- 起床直後・就寝直前の操作負担
- 「同期待ち」や再試行で迷う場面
- 追加したくなった項目や不便な操作

3日後に、これらをもとに受入を最終判定する。
