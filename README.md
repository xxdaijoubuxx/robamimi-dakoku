# ろばみみ打刻

スマートフォンのホーム画面から、起床、就寝、メモの時刻を最小限の操作で記録する
個人用ツールです。

## 現在地

- 完成イメージ：合意済み
- 作業基盤：確定済み
- 実行用WBS：作成済み
- 要件定義：合意済み
- 技術方式：GitHub Pages＋Chrome＋Firebaseで合意済み
- 設計：合意済み
- 試作公開：<https://xxdaijoubuxx.github.io/robamimi-dakoku/>
- Android 16最小試作：Chromeショートカット方式で合格
- Firebase基盤：本人専用Security Rulesの公開とローカル自動試験が完了
- 端末内DB：記録、同期、設定、診断ログの構造と旧試作記録の移行を実装済み
- 起床・就寝：端末内保存と同期待ちを分けた結果画面を実装済み
- メモ：空本文の先行保存、250ミリ秒の自動保存、離脱時保存、失敗時再試行を実装済み
- 履歴：打刻時の現地日付ごとに、時刻・種類・メモ本文・要対応の同期状態を最新順で表示
- 履歴編集：日時・種類・本文の自動保存、失敗表示と再試行を実装済み
- 削除・復元：確認つき論理削除と、30日以内の一件ずつの復元を実装済み
- 端末内データと日常画面：WBS 7.4完了
- 初回認証：本人Google UIDを端末設定へ保持し、未設定端末からの日常画面直開きを防止
- 新規記録の同期：端末保存後に同じ記録IDでFirestoreへ送り、Android実機で同期完了を確認済み
- 同期状態：同期待ち・同期済み・同期失敗・再ログインが必要、を端末内へ保持して表示
- 再同期：起動時・オンライン復帰時の自動同期と履歴からの手動再試行を実装し、既存2件の実機同期を確認済み
- Firebaseからの復元：端末にない記録だけを同じIDで取得し、シークレットタブの新端末相当環境で確認済み
- 変更同期：Firebase側が最後の同期版のままの場合だけ修正・論理削除を反映し、実機と新端末相当環境で確認済み
- 競合解決：両側の内容を保持・比較し、選んだ版へ収束する導線を通常・シークレット環境で確認済み
- 耐障害試験：通信断・オンライン復帰・認証切れ・再認証・多重再送で欠落と重複がないことを実機確認済み
- 認証と同期：WBS 7.5完了
- 初回設定：使い方・Google本人確認・信頼済み端末・次工程を分けて表示し、通常Chrome実機で確認済み
- オフライン準備：Service Workerの公開ファイル全件を検査し、通常Chrome実機で準備完了を確認済み
- ホーム画面追加：通常Chromeで起床・就寝・メモ・履歴を順に確認する案内を実機確認済み
- 読み込み表示：更新ファイルの取得中も白画面にせず、全入口のHTMLで状態を表示
- 初回テスト：専用記録の端末保存・Firebase同期・論理削除・削除同期を設定画面から実機確認済み
- 再設定：設定入口を開き直しても既存履歴が維持されることを実機確認済み
- 初回設定とホーム画面追加：WBS 7.6完了
- CSV生成：削除済みを除く全件を最新順に並べ、日時・種類・本文をUTF-8（BOM付き）で生成
- CSV試験：日本語、カンマ、引用符、LF/CRLF改行、および混在する本文を検査済み
- CSV保存：履歴画面から出力日入りファイル名で保存し、件数・最新日・最古日を表示。Android 16 Chromeで実ファイルを確認済み
- 状態確認：履歴上部に最終同期を表示し、通信・ログイン・記録件数・同期待ち・失敗・再ログイン・競合・オフライン準備の詳細画面をAndroid実機で確認済み
- 診断ログ：許可した操作名・時刻・アプリ版・成否・エラー分類・必要最小限の記録IDだけを端末内へ保存
- 診断情報：本文・認証情報を含まないJSON保存、30日かつ最新500件への自動整理、確認付きログ消去をAndroid実機で確認済み
- CSV・状態確認・診断：WBS 7.7完了
- 構築中検査：各節と現在の正本で型検査・単体テスト・ビルド、およびGitHub Actions成功を確認
- アプリ更新：新版キャッシュ完了後に「更新する」を表示し、明示操作で切替・再読込。0.1.2→0.1.3で端末内7件の維持を実機確認済み
- 文書反映：Firebase採用、状態・診断・更新要件、競合二択後の編集、通常Chrome前提、実機確認結果を正本へ反映
- 初版候補：型検査、通常テスト72件、Security Rulesテスト12件、本番ビルド、GitHub Actions・Pages公開が成功
- 構築：WBS 7.8完了
- 実環境確認：Android 16のホーム画面から起床・就寝を一回で端末保存・同期できることを確認済み
- メモ実環境確認：空本文の先行保存、入力途中からの追記、本文の自動保存と履歴表示を確認済み
- 圏外実環境確認：機内モードで起床・本文付きメモを端末保存し、同期待ちのまま履歴表示できることを確認済み
- 通信回復確認：通信回復後に履歴を開き直すと、圏外保存した起床・メモが自動同期されることを確認済み
- 再試行確認：連続2回の打刻は2件、同一記録の再試行は何度実行しても1件のまま同期されることを確認済み
- 圏外再試行：圏外で手動同期すると表示が固定される不具合を0.1.4で修正し、Android実機で再試験済み
- 履歴編集確認：メモ本文の追記、日時と種類の変更、自動保存、履歴反映、同期完了を実機確認済み
- 削除・復元確認：確認付き削除、最近削除した記録、復元、履歴への再表示と同期完了を実機確認済み
- CSV実環境確認：履歴の全17件を最新順CSVとして保存できることを確認済み
- 状態・障害表示確認：同期待ち0件、端末内17件、アプリ版0.1.4を確認し、本文・認証情報が診断画面に出ないことを確認済み
- 新端末相当確認：シークレットタブで本人確認後、Firebase上の既存記録を復元できることを確認済み
- アクセス拒否確認：Security Rules自動検査12件で未ログイン・別UID・形式不正などを拒否することを確認済み
- 診断安全性確認：状態画面と診断JSONに本文・Google認証情報が含まれないことを実機確認済み
- 受入判定：4.1〜4.7は合格。4.8の3日間実利用だけ未完了のため、現時点は条件付き合格
- 次の作業：WBS 9.1「ホーム画面への初回設定手順を残す」および3日間の実利用

## 正本

- [`完成イメージ.md`](完成イメージ.md)：利用者から見た完成状態
- [`WBS.md`](WBS.md)：構想から完成までの作業と現在地
- [`要件.md`](要件.md)：初版で満たす要件、対象外、受入条件（合意済み）
- [`方式比較.md`](方式比較.md)：技術方式の比較と採用理由（合意済み）
- [`設計.md`](設計.md)：画面、データ、処理、安全・復旧の設計（合意済み）
- [`開発環境.md`](開発環境.md)：既存環境と追加依存関係の配置
- [`使い方.md`](使い方.md)：初回設定、日常利用、状態確認、更新、3日間試用の手順
- [`実環境確認.md`](実環境確認.md)：Android 16実機での受入確認記録

要件、設計、試験、運用の文書は、それぞれの工程で必要な正本を決めます。小さく保てる
場合はREADMEなどへ統合し、同じ情報を複数の文書へ重複させません。

## 保管方針

- プロジェクト本体と恒久文書：このディレクトリ
- 調査メモや一時成果物：必要になった時点でプロジェクト内に用途を明示して置く
- 秘密情報：プロジェクト文書やGit管理対象には保存しない
- ダウンロードした実データ：設計時に保存場所と取扱いを決める

呼び名と恒久フォルダは「ろばみみ打刻」とし、ワークスペース直下のこのディレクトリを正本とします。
