# ろばみみ打刻 設計

作成日：2026-08-03  
状態：合意済み  
対応WBS：5

## 1. 画面とホーム画面からの導線

状態：合意済み

### 1.1 ホーム画面

初回設定後、Androidのホーム画面に四つの入口を置く。

```text
┌──────┐  ┌──────┐
│ 起床  │  │ 就寝  │
└──────┘  └──────┘

┌──────┐  ┌──────┐
│ メモ  │  │ 履歴  │
└──────┘  └──────┘
```

起床、就寝、メモはそれぞれChromeの「ホーム画面に追加」からWebページショートカットを作る。内部の処理とデータは
一つのWebアプリで共有する。履歴も初版では独立した入口として追加し、日常の記録と確認を迷わず
分けられるようにする。

Chromeの「アプリをインストール」で作るPWAは採用しない。同一配信元の四つを独立してインストール
できなかったためである。ショートカット作成時に表示名を手動で「起床」「就寝」「メモ」「履歴」とする。
準備ページを表示したら、再読み込みせずURLだけを通常の入口へ置き換える。これにより、準備中には記録せず、
作成されたショートカットは`install=1`のない打刻用URLを指す。

四つを独立して追加できるかは、全画面を作る前の最小試作でAndroid 16実機確認する。

### 1.2 起床・就寝

ホーム画面の入口をタップすると、入力画面や確認画面を挟まず、最初に端末内へ記録する。

保存成功時：

```text
┌──────────────────┐
│        起床       │
│                  │
│      07:12        │
│                  │
│  ✓ 記録しました   │
│  ✓ 同期済み       │
└──────────────────┘
```

圏外で端末内保存に成功した場合：

```text
┌──────────────────┐
│        起床       │
│                  │
│      07:12        │
│                  │
│  ✓ 端末に保存済み │
│  ↻ 同期待ち       │
└──────────────────┘
```

- 日時はタップした時点で確定する。
- 画面表示やFirebase同期を待ってから日時を取らない。
- 記録後の画面は自動で閉じることを前提にしない。
- 利用者は結果を確認し、Androidのホーム操作などで画面を離れられる。
- 同種の連続記録がある場合は、成功表示の下に「直前にも起床があります」と小さく表示する。
- 重複の可能性があっても記録を止めたり、確認ボタンを要求したりしない。

端末内保存自体に失敗した場合：

```text
┌──────────────────┐
│   記録できません  │
│                  │
│  端末への保存に   │
│  失敗しました     │
│                  │
│    [もう一度]     │
└──────────────────┘
```

この場合だけ成功扱いにせず、利用者が再試行できるようにする。

### 1.3 メモ

「メモ」をタップした時点で、本文が空の記録を端末内へ保存する。保存後に同じ画面で本文入力欄を
表示する。

```text
┌──────────────────┐
│ メモ       14:23 │
│                  │
│ ┌──────────────┐ │
│ │              │ │
│ │ 任意のメモ   │ │
│ │              │ │
│ └──────────────┘ │
│                  │
│ ✓ 自動保存済み   │
│ ✓ 同期済み       │
└──────────────────┘
```

- 入力欄へ最初からカーソルを置き、すぐ文字入力や音声入力を始められるようにする。
- 保存ボタンは置かない。
- 入力中の本文は端末内へ短い間隔で自動保存する。
- 何も書かず画面を離れても、最初のタイムスタンプを保持する。
- 本文の端末内保存とFirebase同期の状態を区別して表示する。
- 自動保存に失敗した場合は未保存であることを表示し、入力中の文字を可能な限り画面に残す。

### 1.4 履歴

```text
┌──────────────────┐
│ 履歴              │
│ 最終同期 14:25    │
│                  │
│ 8月3日            │
│ 14:23 メモ        │
│       頭痛が少し… │
│                  │
│ 07:12 起床        │
│                  │
│ 8月2日            │
│ 23:48 就寝        │
│                  │
│ [CSVを保存]       │
└──────────────────┘
```

- 新しい記録から表示する。
- 日付見出しは読みやすさのために置くが、一日単位の集計はしない。
- 同期待ちや失敗がある記録にだけ状態を表示する。
- 記録を選ぶと、日時、種類、本文の編集画面を開く。
- 編集内容も端末内へ自動保存する。
- 削除は編集画面から行い、確認画面を一度挟む。
- CSVは履歴画面から全件を保存する。

### 1.5 初回設定

初回だけ、通常のアプリ入口で次の順に設定する。

```text
1. このツールの説明を確認
2. Googleアカウントでログイン
3. 端末内へのオフライン保存を有効化
4. 起床ショートカットを作り、表示名を「起床」にする
5. 就寝ショートカットを作り、表示名を「就寝」にする
6. メモショートカットを作り、表示名を「メモ」にする
7. 履歴ショートカットを作り、表示名を「履歴」にする
8. テスト記録を一件作成して削除
9. 設定完了
```

- 各入口が追加済みかをWebアプリだけで完全に判定できない可能性があるため、追加操作を一つずつ
  案内し、利用者が完了を進める方式とする。
- Googleログイン前には日常用入口を追加させない。
- 共有端末ではなく本人の端末であることを確認してから、永続的なオフライン保存を有効にする。
- 初回設定をやり直しても既存データを消さない。

### 1.6 Chrome・Webショートカット固有の早期検証

次を最小試作で確認し、成立しない場合は設計または方式選定へ戻る。

1. 同じGitHub Pages配下の四つをWebショートカットとして独立したホーム画面アイコンにできる。
2. 追加時に四つの表示名を手動設定できる。
3. 各アイコンの名前、見た目、起動先を区別できる。
4. アイコンを繰り返しタップするたびに、新しい記録処理が一度だけ実行される。
5. 前回の結果画面がChrome内に残っていても、新しいタップを検出できる。
6. 圏外でアイコンをタップしても画面が開き、端末内へ保存できる。
7. 四つの入口が同じ認証状態と端末内データを共有できる。

2026年8月3日のAndroid 16実機確認では、四つのWebショートカットを「起床」「就寝」「メモ」「履歴」の
短い名前で追加できた。ホーム画面からの1タップ、起床の繰り返し打刻、結果画面の再読み込み抑止、四入口の
端末内データ共有、機内モードかつWi-Fiオフでの打刻と履歴表示がすべて成立した。確認後、Firebase未同期の
試作記録は全削除した。この結果によりChromeショートカット方式を継続する。

## 2. 記録データ

状態：合意済み

### 2.1 考え方

一件の打刻を一件の記録として扱う。起床と就寝を一組にしたり、一日分を一件にまとめたりしない。
これにより、同じ日に複数回記録しても元の事実を保てる。

利用者が扱う中心データは、日時、種類、本文の三つだけにする。同期や重複防止に必要な管理情報は
アプリ内部で持ち、通常の履歴やCSVには出さない。

### 2.2 一件の記録

| 項目 | 用途 | 利用者に表示 |
|---|---|---|
| 記録ID | 再送時の二重登録を防ぐ固有番号 | しない |
| 種類 | `wake`、`sleep`、`memo`のいずれか | 起床、就寝、メモとして表示 |
| 打刻日時 | ホーム画面をタップした瞬間 | する |
| タイムゾーン | 打刻時の地域・時差を保持 | 必要に応じて日時へ反映 |
| 本文 | メモの任意本文。起床・就寝では空 | する |
| 作成日時 | 端末内に最初に保存した時刻 | 通常はしない |
| 更新日時 | 本文や日時などを最後に変更した時刻 | 通常はしない |
| 変更版 | 同期時に新旧を判定する番号 | しない |
| 削除日時 | 削除済みであることを他端末へ伝える | しない |
| 作成端末ID | 機種変更時の競合確認に使う端末の識別子 | しない |

記録IDは端末内で作り、Firebaseでも同じIDを文書IDとして使う。同じ記録を何度送っても同じ場所を
更新するため、通信再試行による重複を防げる。

### 2.3 日時

- 内部では時刻を世界共通の基準で保存し、打刻時のタイムゾーンも別に保持する。
- 履歴とCSVでは、打刻した場所の現地時刻を表示する。
- 初版の画面表示は秒を省き、分まで表示する。
- 内部には秒以下も保持し、連続タップの順番を失わない。
- 日時をあとから修正した場合も、作成日時と更新日時を別に残す。

### 2.4 種類と本文

- 種類の内部値は`wake`、`sleep`、`memo`に固定する。
- 表示名は「起床」「就寝」「メモ」とする。
- 本文は空文字を許可する。
- 起床・就寝の本文は初版では空とし、画面から入力させない。
- メモ本文には改行、カンマ、引用符、日本語を保存できる。
- 本文は前後の空白をアプリが勝手に削らず、入力内容を保つ。

### 2.5 削除

履歴で削除しても、最初から記録が存在しなかった状態にはしない。削除日時を設定した「削除済み記録」
として端末内に残し、その削除をFirebaseへ同期する。通常の履歴とCSVからは除外する。

削除の同期が完了した後、削除済み記録を端末内・Firebaseにどのくらい残すかは、WBS 5.6の
バックアップ・復旧設計で決める。

### 2.6 端末内の保存領域

ChromeのIndexedDBに、役割を分けて保存する。

| 保存領域 | 内容 |
|---|---|
| records | 打刻記録と削除済み記録 |
| sync | 各記録の同期済み版、同期待ち、失敗、競合状態 |
| settings | 本人のUID、端末ID、最終同期時刻、初回設定の進行状態 |

認証用の秘密情報を独自に保存せず、Firebase Authenticationが管理する認証状態を利用する。

### 2.7 Firebase上の配置

本人のUIDごとに記録を分離する。

```text
users
└─ {本人のUID}
   └─ records
      └─ {記録ID}
         ├─ kind
         ├─ occurredAt
         ├─ timezone
         ├─ body
         ├─ createdAt
         ├─ updatedAt
         ├─ revision
         ├─ deletedAt
         └─ deviceId
```

他人のUID配下は読み書きできないよう、Firebaseのアクセス規則で制限する。

## 3. 処理と同期

状態：合意済み

### 3.1 起床・就寝の打刻順序

入口をタップした後は、次の順序で処理する。

```text
ホーム画面をタップ
        ↓
その瞬間の日時を取得
        ↓
記録IDを作成
        ↓
端末内へ保存 ──失敗──→ 失敗表示・再試行
        ↓成功
打刻成功を表示
        ↓
オンラインならFirebase同期を試行
        ↓
同期済み／同期待ち／失敗を表示
```

Firebaseからの応答を待ってから打刻成功にしない。端末内保存が成功した時点で、日常の打刻は完了と
する。同期中に画面を離れても、端末内の同期待ち記録は残る。

### 3.2 一回の起動で一回だけ記録する

起床、就寝、メモの入口URLを開くたびに新しい記録IDを作る。保存後はブラウザ履歴を増やさず、同じ入口の
URLを`?done=1`付きへ置き換える。結果画面を手動で再読み込みしても`done=1`を検出して新しい記録を
作らない。ホーム画面のショートカットは`done=1`のない入口URLを開くため、次の本当のタップでは
新しい一件を作る。

画面再描画やService Workerの更新だけでは保存処理を再実行しない。準備用の`?install=1`と記録済みの
`?done=1`では打刻せず、どちらも付かない入口URLだけで一回保存する。

利用者による本当の連続タップは、それぞれ別の起動・別の記録として残す。アプリ都合の二重実行だけを
防ぎ、利用者の二回目を勝手に消さない。

### 3.3 メモ本文の保存

メモ入口では、空本文の記録を端末内へ保存して成功表示を出した後、入力欄を使えるようにする。

- 本文が変更されるたびに短時間で端末内保存を予約する。Enterなどの特別な確定操作は要求しない。
- 入力が続く間は書き込みをまとめ、最後に本文が変更されてから約250ミリ秒後に保存する。
- 日本語入力では変換途中の未確定文字を保存契機にせず、本文として確定した変更を保存する。
- 画面が非表示になる、入力欄から離れる、アプリを閉じる兆候がある場合は即時保存を試みる。
- 端末内保存が完了するまで「保存中」、完了後は「保存済み」と表示する。
- 保存失敗時は「未保存」と表示し、再試行できるようにする。
- 本文変更のたびに打刻日時を変えず、更新日時と変更版だけを進める。

ブラウザが強制終了した瞬間などは最後のごく短い入力を保存できない可能性があるため、250ミリ秒という
短い間隔と画面離脱時保存を組み合わせる。最初の空メモとタイムスタンプは本文入力より先に保存済みである。

途中で入力が止まった場合は、その時点の本文を同じ一件へ保存する。たとえば「頭痛」を保存したあとに
「がした」を続けると、別のメモを追加せず、元の一件を「頭痛がした」へ更新する。

端末内保存とFirebase同期の間隔は分ける。端末内には約250ミリ秒でこまめに保存するが、Firebaseへは
もう少し長く入力が止まったとき、画面を離れたとき、または次回起動時に同じ記録IDで送る。これにより、
入力途中を端末内で守りながら、Firebaseへ一文字ずつ送信することを避ける。

### 3.4 同期を始めるタイミング

次の場合に同期待ち記録の送信とFirebase側の変更取得を試みる。

- 起床、就寝、メモ、履歴のいずれかを開いたとき
- オンライン状態へ変わったことを、開いているアプリが検知したとき
- 利用者が履歴画面で「同期を再試行」を押したとき

Chromeを閉じたまま、通信回復直後に同期することは初版では保証しない。次回いずれかの入口を開いた
ときには、利用者の追加操作なしで同期を始める。

### 3.5 Firebaseへ新規記録を送る

端末内の記録IDをFirebaseの文書IDにも使う。

```text
端末内：record-123（同期待ち）
              ↓
Firebase：record-123
```

通信結果が不明で再送しても、同じIDの場所へ送るため二件にはならない。新規記録の送信が確認できたら、
端末内の同期済み版を更新し、「同期済み」にする。

### 3.6 修正・削除を同期する

端末内で修正または削除する際、最後に同期した変更版を記録しておく。Firebase上の変更版がそのままなら
端末側の新しい版を反映する。

Firebase側が先に変更されていた場合は、自動で上書きせず競合として保持する。

```text
端末版：14:23「頭痛」
Firebase版：14:23「薬を飲んだ」
                 ↓
「確認が必要な変更」として両方を保持
```

履歴画面で競合を知らせ、端末版、Firebase版、または必要に応じて編集した内容のどれを残すか選べる
ようにする。初版では複数端末の日常利用を想定しないため、通常は発生しない例外画面である。

### 3.7 Firebase側から復元する

新端末の初回ログイン時は、本人のFirebase記録を取得して端末内へ保存する。既存端末の通常同期では、
端末内の同期待ちを先に保護してからFirebase側の変更を取り込む。

- 端末だけにある新規記録は送信する。
- Firebaseだけにある記録は端末へ追加する。
- 同じIDで片側だけが新しい場合は新しい版を反映する。
- 両側が別々に更新されていれば競合として両方を保持する。
- 削除済み記録も同じ規則で同期し、別端末で勝手に復活させない。

### 3.8 履歴とCSV

履歴とCSVは、まず端末内の記録から作る。Firebaseの読み込み完了を待たなければ表示やCSV保存が
できない設計にはしない。

- 履歴を開くと端末内データをすぐ表示し、同期は並行して行う。
- 同期で新しい変更が届いたら一覧を更新する。
- CSVには生成開始時点の端末内全件を最新順で含める。
- 同期待ちの記録も含めるが、同期状態の列は利用者向けCSVへ追加しない。

## 4. 認証と権限

状態：合意済み

### 4.1 守る対象と境界

GitHub Pages上の画面とJavaScriptは公開される。公開コードを秘密にすることでデータを守るのではなく、
Firebase AuthenticationとFirestore Security Rulesで本人の記録を守る。

```text
公開してよいもの
├─ HTML、CSS、JavaScript
├─ Firebaseの接続先設定
└─ データの項目構造

公開しないもの
├─ 本人の打刻記録
├─ Googleの認証情報
├─ 秘密鍵・管理者資格情報
└─ ダウンロードした実データ
```

FirebaseのWeb接続設定は接続先を示す情報であり、それだけでは本人データを読めない。秘密鍵やFirebase
管理用の資格情報はGitHub、ブラウザ、設計文書へ置かない。

### 4.2 初回ログイン

- 初回設定にはオンライン接続を必要とする。
- Firebase Authenticationを使い、Googleアカウントでログインする。
- GitHub PagesではChromeの第三者ストレージ制限により単純なリダイレクト認証が成立しないため、
  利用者がボタンを押した直後にGoogleログインのポップアップを開く。日常打刻では再表示しない。
- 指定した本人のFirebase UIDだけを、このツールの所有者として許可する。
- 別のGoogleアカウントでログインした場合は、記録画面へ進めず「このアカウントは利用できません」と
  表示する。
- 本人確認後にだけ、オフライン利用の有効化とホーム画面入口の追加へ進む。

本人のUIDはパスワードではないため、アクセス規則に使用してよい。UIDを知っているだけでは、本人の
Google認証を通過できない。

2026年8月3日にGoogleログインで確認した本人UIDは`p8H8qiKvh3f1gWgPlowG0mfTHQH2`とする。Webアプリと
Firestore Security Rulesの両方でこのUIDだけを許可し、別UIDでは画面上でも利用不可を明示する。

### 4.3 日常の打刻とログイン状態

初回設定後は、起床・就寝・メモのたびにGoogleログインを要求しない。Firebaseのログイン状態と、
端末内に保存した所有者UIDが一致することを通常状態とする。

圏外またはFirebaseの認証確認が一時的にできない場合でも、初回設定済みの端末では端末内へ打刻する。
このときFirebase同期は保留し、次回オンライン時に認証状態を確認してから送る。

認証が失効している場合：

```text
端末内への打刻：続ける
Firebaseへの同期：止める
画面表示：再ログインが必要
端末内データ：消さない
```

再ログインしたUIDが元の所有者UIDと一致した場合だけ、同期待ちを送信する。別UIDでは混ぜない。

### 4.4 Firestore上のアクセス制御

Firestore Security Rulesでは、少なくとも次をサーバー側で検査する。

- Googleログイン済みである。
- 認証UIDが指定した本人UIDと一致する。
- `/users/{本人UID}/records/{記録ID}`以外を読み書きしない。
- URL内のUIDと認証UIDが一致する。
- 記録の種類が`wake`、`sleep`、`memo`のいずれかである。
- 必須項目の型が正しく、想定外の管理項目を追加していない。

未ログイン、別UID、形式不正の読み書きは拒否する。画面側の非表示だけに頼らず、Firebase側で拒否する。

### 4.5 URLを直接開いた場合

- 初回設定前に打刻URLを直接開いても、記録を作らず初回設定へ案内する。
- 初回設定済み端末では、ホーム画面入口と同じように打刻する。
- 別端末や別ブラウザでURLを開いただけでは、本人データを表示・記録しない。
- 新端末では、Googleログインと復元を済ませてから入口を追加する。

### 4.6 ログアウトと端末内データ

ログアウトはFirebase同期を停止するが、それだけで端末内記録を削除しない。同期前の記録を誤って
失わないためである。

端末内データの消去は、ログアウトとは別の危険な操作として扱う。実行前に、未同期件数、最終同期時刻、
消去後は端末内から復元できないことを表示し、明示的な確認を求める。具体的な復旧・消去手順は
WBS 5.6で設計する。

### 4.7 端末自体の保護

初回設定済みのスマートフォンでは、圏外打刻のためにGoogleへ毎回問い合わせない。そのため、端末の
ロックを解除して操作できる人はホーム画面から打刻できる。初版ではAndroidの画面ロックを、端末内の
日常操作を守る境界とする。

履歴の閲覧時だけ毎回追加認証する機能は初版に含めない。必要性が実利用で判明した場合に検討する。

## 5. 失敗と回復

状態：合意済み

### 5.1 失敗を二種類に分ける

利用者への表示は、まず次の二種類を区別する。

| 状態 | 打刻の扱い | 表示 |
|---|---|---|
| 端末内保存に成功、Firebase同期に失敗 | 打刻済み | 「端末に保存済み・同期待ち」 |
| 端末内保存に失敗 | 未記録 | 「記録できません」 |

Firebaseへ送れないだけなら、日常の打刻を失敗扱いにしない。一方、端末内にも残せなかった場合は、
同期済みのような表示をせず、未記録であることを明確にする。

色だけで成否を区別せず、記号と文章を併用する。

### 5.2 端末内保存に失敗

打刻開始時に確定した記録IDと日時を画面内に保持し、「もう一度」を押すと同じID・同じ日時で保存を
再試行する。再試行時刻へ勝手に置き換えない。

```text
記録できません
起床 07:12

端末への保存に失敗しました
[もう一度]
```

再試行できない場合も、利用者が別途残せるよう、種類と予定していた日時を画面に表示し続ける。

考えられる原因として、Chromeの保存領域が利用できない、容量不足、データベース更新失敗などを区別して
ログへ残す。利用者向けには、原因に応じてChromeの再起動、空き容量確認、履歴画面の診断など、実行可能な
対処だけを案内する。

### 5.3 Firebase同期に失敗

- 端末内記録を保持し、同期待ちまたは同期失敗として表示する。
- 次回起動時とオンライン検知時に自動再試行する。
- 履歴画面に手動の「同期を再試行」を用意する。
- 失敗回数だけで端末内記録を削除しない。
- 認証切れ、権限拒否、通信不能、Firebase側障害、利用上限などを内部で区別する。
- 自動回復できない認証・権限・利用上限では、必要な操作を履歴画面に表示する。

ブラウザのオンライン表示だけで成功と判断せず、Firebaseからの実際の応答で同期結果を決める。

### 5.4 メモ本文の保存に失敗

- 最初の空メモが端末内保存済みなら、タイムスタンプ自体は保持する。
- 本文が未保存であることを入力欄の近くに表示する。
- 入力中の文字を画面から消さず、再試行できるようにする。
- 画面を離れる前に、未保存本文があることを可能な範囲で警告する。
- 回復できない場合に備え、本文を選択・コピーできる状態を保つ。

保存に失敗した本文を、保存済みであるように表示しない。

### 5.5 重複と多重実行

- 一つの起動処理が内部都合で複数回走っても、同じ起動IDでは一件だけ保存する。
- Firebaseへの同じ記録IDの再送では新しい文書を増やさない。
- 利用者がホーム画面を本当に二回タップした場合は、別の起動IDとして二件残す。
- 短時間に同種が並んだ場合は履歴と結果画面で重複可能性を知らせる。
- 自動削除や自動統合はしない。

### 5.6 変更の競合

端末版とFirebase版が別々に変更されている場合、どちらかを自動で捨てない。両方の内容と更新日時を
端末内へ保存し、履歴に「確認が必要」と表示する。

利用者が残す内容を選ぶまで、通常の同期対象からその記録を外す。他の正常な記録の同期は続ける。
競合一件のために全同期を止めない。

### 5.7 端末時計のずれ

打刻には端末時刻を使うため、端末時計が誤っていれば記録もずれる。サーバー時刻へ黙って置き換えず、
大きなずれを検知できた場合は注意を表示する。記録後は履歴から正しい日時へ修正できる。

### 5.8 GitHub Pages・アプリ更新の失敗

- 初回設定時に、圏外でも起動できるアプリ本体が保存されたことを試験する。
- GitHub Pagesへ接続できなくても、保存済みのアプリ本体で打刻を継続する。
- 新版の取得に失敗した場合は、動作確認済みの旧版を使い続ける。
- アプリ更新時に端末内の打刻データを削除しない。
- 端末内データ形式の更新は、既存データを残したまま移行し、失敗時に初期化でごまかさない。

GitHub Pagesを一度も開いていない新端末や、Chromeのサイトデータを消去した端末では、オフライン起動は
できない。オンラインで再設定し、Firebaseから同期済み記録を復元する。

### 5.9 CSV出力の失敗

CSV生成やダウンロードに失敗しても、元の記録を変更しない。失敗理由を表示し、再試行できるようにする。
出力成功と判断する範囲は、ファイル生成とAndroidへの保存要求が正常に完了したところまでとし、利用者が
実際にファイルを開けることは受入試験で確認する。

### 5.10 状態表示のまとめ

| 表示 | 意味 | 利用者の対応 |
|---|---|---|
| 記録しました・同期済み | 端末とFirebaseに保存済み | なし |
| 端末に保存済み・同期待ち | 打刻済み、Firebase未反映 | 通常はなし |
| 同期に失敗 | 端末にはあるが自動回復できていない | 履歴で詳細確認・再試行 |
| 再ログインが必要 | 端末にはあるが認証切れ | オンライン時に再ログイン |
| 記録できません | 端末内にも未保存 | その場で再試行 |
| 確認が必要な変更 | 端末版とFirebase版が競合 | 履歴で残す内容を選択 |

## 6. バックアップと復旧

状態：合意済み

### 6.1 三つを区別する

| 仕組み | 守るもの | 守れないもの |
|---|---|---|
| 端末内保存 | 圏外時を含む直近の打刻 | 端末故障・サイトデータ消去 |
| Firebase同期 | 端末故障・機種変更 | 同期前の記録、Firebase側にも反映された誤変更 |
| CSV出力 | 利用者が持ち出した時点の全記録 | 出力後に追加・変更した記録 |

Firebase同期はスマートフォン故障への備えであり、過去の任意時点へ戻せる完全なバックアップではない。
CSVはデータを自由に持ち出す手段だが、利用者へ定期作業として強制しない。

### 6.2 スマートフォン故障・機種変更

同期済み記録はFirebase側へ残し、新しいスマートフォンでは同じデータへ接続して記録を続ける。
旧端末の状態を戻すための特別な復元作業は利用者に要求しない。

```text
1. オンラインでGitHub Pagesの初回設定を開く
2. 元と同じGoogleアカウントでログイン
3. Firebase上の既存データへ接続
4. 新しい端末IDを作る
5. 履歴表示と圏外利用に必要なデータをアプリが自動取得
6. 圏外起動を試験
7. 四つの入口をホーム画面へ追加
8. テスト記録を作成・同期・削除
9. 利用再開
```

新端末からの記録は、Firebase上の既存記録を置き換えず、同じ本人データへ新しい記録として追加する。
データ量は小さい想定のため、初回接続時には同期済み記録を自動取得して端末内にも保持するが、これは
履歴・圏外利用・CSVのための作業用コピーである。安全のために残す本体はFirebase側の同期済み記録とする。

故障端末でFirebaseへ未同期だった記録は復元できない。最終同期時刻と未同期件数を履歴に常時確認できる
ようにし、通信可能な起動時には自動同期して未同期期間を短くする。

### 6.3 Chromeのサイトデータ消去・再設定

Chromeのサイトデータを消すと、端末内記録、同期待ち、設定、オフライン用アプリ本体が消える。
Firebaseへ同期済みの記録は、同じGoogleアカウントで初回設定をやり直せば再接続できる。

アプリ内から端末内データを消去する場合は、次を満たすまで実行させない。

- 同期待ちが0件である。
- 同期失敗・競合が0件である。
- 最終同期に成功している。
- 消去後はFirebaseへ再接続してデータを取得する必要があることを表示した。
- 利用者が明示的に最終確認した。

Chromeの設定画面から利用者が直接サイトデータを消す操作までは、アプリ側で防げない。

### 6.4 誤削除

削除確認後の記録は通常履歴とCSVから除外するが、Firebaseと端末内では削除済みとして30日間保持する。
設定・診断画面の「最近削除した記録」から、30日以内なら復元できるようにする。

- 復元すると変更版を一つ進め、通常履歴へ戻す。
- 復元もFirebaseへ同期する。
- 30日経過後は、同期済みで競合がない削除済み記録を完全削除の候補にする。
- 完全削除前に、別端末の古い状態から記録が復活しないことを同期情報で確認する。

保持期間は30日とする。

### 6.5 Firebase側の障害・消失

Firebaseへ接続できない間も端末内の記録は使い続ける。Firebaseプロジェクト自体の誤削除、アカウント
喪失、無料枠停止などでは、端末内データと過去に出力したCSVが残る範囲となる。

初版では有料の世代バックアップを前提にしない。Firebaseプロジェクト作成後、利用できる公式の
バックアップ・エクスポート条件を確認し、無料で無理なく使えるものだけ運用へ採用する。

Firebase接続を作り直す必要がある場合に備え、データ構造、アクセス規則、設定項目、再構築手順は
GitHub上のコードと運用文書を正本として残す。秘密情報や実データはGitHubへ保存しない。

### 6.6 CSVによる持ち出し

- 履歴画面からいつでも全件を出力できる。
- 出力時に件数、最新日時、最古日時を表示する。
- CSVへは削除済み記録と内部同期情報を含めない。
- 初版ではCSVからFirebaseへ戻すインポート機能を作らない。
- CSVがあっても自動復元できるとは案内せず、人が読める持ち出しデータとして位置付ける。

将来インポートが必要になった場合は、重複、時刻、削除済み記録の扱いを別途要件定義してから追加する。

### 6.7 ソースコードと設定の復旧

- ソースコードの正本はGitHubリポジトリとする。
- GitHub PagesはGitHub上の正本から再公開できるようにする。
- Firebaseのデータ構造とSecurity RulesはコードとしてGitHubへ保存する。
- Firebase管理画面で人が行う設定は、値そのものではなく設定手順を運用文書へ残す。
- Firebase Web SDK用の公開APIキーと接続設定はGitHubへ保存する。Firebase以外のAPIキー、秘密鍵、
  アクセストークン、本人の実データはGitHubへ保存しない。

## 7. ログと障害確認

状態：合意済み

### 7.1 利用者が確認できる状態

履歴画面から「状態を確認」を開けるようにする。

```text
アプリ状態

アプリ版          1.0.0
通信              オンライン
Googleログイン     有効
端末内記録         124件
同期待ち           0件
同期失敗           0件
確認が必要な変更   0件
最終同期           8月3日 14:25
オフライン準備     完了

[同期を再試行]
[診断情報を保存]
```

正常時は細かな情報を日常画面へ並べず、履歴上部に最終同期時刻だけを表示する。未同期、失敗、競合、
再ログインなど利用者の対応が必要な場合は、履歴上部から状態確認へ誘導する。

### 7.2 端末内ログ

問題の原因をあとから確認できるよう、次の出来事を端末内へ記録する。

- アプリ起動と入口の種類
- 端末内保存の成功・失敗
- メモ本文保存の成功・失敗（本文そのものは記録しない）
- 同期開始、成功、失敗、再試行
- Googleログイン、再認証、ログアウトの結果
- 競合の検出と解決
- CSV生成の成功・失敗
- アプリと端末内データ形式の更新結果
- Service Workerとオフライン準備の状態

一件のログには、日時、アプリ版、処理名、成功・失敗、エラー分類、必要最小限の記録IDを含める。

### 7.3 ログへ入れないもの

- メモ本文
- Googleアカウントの認証トークン
- パスワード、秘密鍵、管理者資格情報
- Firebaseの非公開管理情報
- CSVの内容
- 不要な端末情報や行動分析情報

利用状況を外部へ自動送信するアクセス解析や障害解析サービスは、初版では導入しない。診断ログは
端末内だけに置き、利用者が明示的に保存・共有した場合だけ外へ出る。

### 7.4 保持と整理

- 通常ログは30日間または500件のうち、先に達した範囲まで保持する。
- 古い通常ログは自動削除する。
- 未解決の同期失敗・競合に関する情報は、問題が解決するまで通常ログとは別に保持する。
- ログ整理で打刻記録を削除しない。
- 利用者は状態確認画面から診断ログだけを消去できる。

### 7.5 診断情報の保存

「診断情報を保存」では、本文や認証情報を含まないテキストまたはJSONを生成する。少なくとも次を含める。

- アプリ版とデータ形式版
- 生成日時
- オンライン・認証・オフライン準備の状態
- 記録件数、同期待ち件数、失敗件数、競合件数
- 最終同期時刻
- 直近の処理ログ

利用者がCodexへ問題を相談する際、画面を説明し直さず、この診断情報または状態画面のスクリーンショットを
共有できるようにする。実データ本文を含まないため、通常の履歴やCSVより安全に共有できる。

### 7.6 Firebase側で確認するもの

Firebase管理画面では、本人UID配下の記録件数、最新更新、Security Rulesによる拒否、利用量を確認できる
ようにする。日常的に管理画面を見る運用にはせず、端末側だけでは原因が分からない場合に使用する。

Firebase側の確認手順は、構築後の運用文書へ画面名と確認項目を残す。管理者資格情報や実データは
診断ログへ含めない。

## 8. 技術構成

状態：合意済み

### 8.1 採用候補

| 役割 | 技術 | 選ぶ理由 |
|---|---|---|
| 開発・ビルド | Vite、TypeScript | 小さく始められ、GitHub Pages用の静的ファイルを生成できる |
| 画面 | HTML、CSS、TypeScript | 画面数が少なく、Reactなどの追加フレームワークを必要としない |
| 端末内DB | IndexedDB、`idb` | ブラウザ標準DBを小さな補助ライブラリで安全に扱う |
| オフライン起動 | 独自Service Worker | 複数PWAの入口と更新・旧版維持を見通せる形で管理する |
| 本人確認 | Firebase Authentication | Googleアカウントで本人を識別する |
| 端末外DB | Cloud Firestore | 同期済み記録を端末外へ保持する |
| テスト | Vitest、必要箇所のみPlaywright | 処理単体とブラウザ操作を分けて確認する |
| 公開 | GitHub Actions、GitHub Pages | GitHub上の正本から同じ手順で再公開する |

Firebase SDKはWeb向けのモジュール版を使用する。依存関係は`package.json`とロックファイルを正本にし、
ブラウザへ読み込む外部CDNの版へ暗黙に依存しない。

### 8.2 入口と処理本体

入口ごとに小さなHTMLを持たせるが、記録処理、同期処理、画面部品、データ定義は共有する。

```text
/
├─ setup/       初回設定
├─ wake/        起床の開始URLとManifest
├─ sleep/       就寝の開始URLとManifest
├─ memo/        メモの開始URLとManifest
├─ history/     履歴の開始URLとManifest
└─ 共通処理     端末DB、同期、認証、表示
```

四つの入口は異なる開始URL、表示名、アイコンを持つWebショートカットとして作る。同じGitHub Pagesの
配下に置くため、認証状態、IndexedDB、Service Worker、処理本体を共有する。Web App Manifestは
ショートカット作成時の名称・アイコン候補とオフラインWebアプリの情報に使うが、四つを独立した
インストール済みPWAとして扱うことは前提にしない。

### 8.3 コードの責務

```text
src/
├─ model/          記録と管理情報の型・検証
├─ local/          IndexedDBへの保存・取得・移行
├─ sync/           Firebaseとの送受信・競合判定
├─ auth/           Googleログインと所有者確認
├─ actions/        起床・就寝・メモの一回実行
├─ screens/        初回設定、結果、メモ、履歴、状態確認
├─ csv/            CSV生成
└─ diagnostics/    状態表示と本文なしログ
```

画面からFirebaseへ直接書き込まず、必ず端末内保存と同期処理を通す。起床・就寝・メモで同じ保存処理を
共有し、入口ごとに実装を複製しない。

### 8.4 GitHubへ置くもの

- ソースコード
- テスト
- Web App Manifestとアイコン
- Firestore Security Rules
- GitHub Pagesの公開手順
- Firebase初回設定と復旧の手順
- データ構造の定義

GitHubへ、実データ、CSV、診断ログ、認証トークン、秘密鍵、管理者資格情報は置かない。

### 8.5 検査の分担

| 検査 | 確認するもの |
|---|---|
| TypeScript・静的検査 | 型、未使用、基本的なコード不整合 |
| 単体テスト | 記録作成、日時、CSV、変更版、競合判定 |
| ブラウザテスト | IndexedDB保存、画面遷移、Service Worker、再読み込み |
| Firebase Emulatorまたは規則テスト | 未ログイン、別UID、形式不正、本人UID |
| Android 16実機 | 四つのアイコン、1タップ、圏外、再同期、CSV保存 |

実機でしか確かめられないPWAの独立アイコンと繰り返し起動を、構築の後半まで保留しない。共通処理を
広げる前に最小試作で確認する。
