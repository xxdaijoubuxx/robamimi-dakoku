# ろばみみ打刻 開発環境

確認日：2026-08-03  
状態：依存関係インストール・基本検査済み

この文書は、既存の開発道具と、このプロジェクトのために追加するものの配置を明確にする。
インストールやPATH変更を行う前に更新し、意図しないグローバル環境の増殖を防ぐ。

## 既存環境をそのまま使うもの

| 道具 | 版・場所 | 今回の扱い |
|---|---|---|
| Node.js | `v22.23.2`、`/usr/bin/node` | システム既存版を使用 |
| npm | `10.9.8`、`/usr/bin/npm` | システム既存版を使用 |
| npx | `/usr/bin/npx` | 必要時も配置を確認してから使用 |
| Git | `/usr/bin/git` | ローカル履歴に使用 |
| GitHub CLI | `/usr/bin/gh` | GitHubリポジトリ作成・公開時に使用候補 |
| Google Chrome | `/usr/bin/google-chrome` | ローカル・実機向けWeb確認に使用 |
| OpenJDK 17 | `17.0.19`、`/usr/lib/jvm/java-17-openjdk-amd64/` | OSの既定Javaとして維持 |

Node.jsはnvm、asdf、Homebrewなどのユーザー別配置ではなく、`/usr/bin`のシステム版である。
npmのグローバルprefixは`/usr`、グローバルモジュール置き場は`/usr/lib/node_modules`。
既存のグローバルnpmパッケージとして、npmとcorepackだけが確認できた。

今回、Node.js、npm、Git、GitHub CLI、Chromeを新規インストールしたり、PATHを変更したりしない。

## 現在のPATH

今回のために追加したPATHはない。主な順序は次のとおり。

```text
/home/mariko/.local/bin
Codexの実行用パス
/usr/local/sbin
/usr/local/bin
/usr/sbin
/usr/bin
/sbin
/bin
/snap/bin
```

`/home/mariko/.local/bin`は既存状態で二度含まれている。今回は修正対象にせず、プロジェクト側から
新しいPATHを追加しない。

## プロジェクト内へ追加した依存関係

すべて`/home/mariko/m-sunaba/ろばみみ打刻/node_modules/`へローカルインストールした。
`npm install -g`やsudoは使わない。

| パッケージ | 版 | 用途 |
|---|---:|---|
| `vite` | 8.2.0 | 開発サーバーとGitHub Pages用ビルド |
| `typescript` | 7.0.2 | 型検査 |
| `vitest` | 4.1.10 | 単体テスト |
| `idb` | 8.0.3 | IndexedDBの小さな操作補助 |
| `firebase` | 12.17.0 | GoogleログインとFirestore接続 |
| `firebase-tools` | 15.24.0 | Firestore Emulatorの起動（開発依存のみ） |
| `@firebase/rules-unit-testing` | 5.0.1 | Security Rulesの自動試験 |

インストールにより次が作られる。

```text
/home/mariko/m-sunaba/ろばみみ打刻/node_modules/
/home/mariko/m-sunaba/ろばみみ打刻/package-lock.json
```

`node_modules/`はGit管理外、`package-lock.json`は依存版を再現する正本としてGit管理対象とする。
npmスクリプト実行中だけ`node_modules/.bin`が内部利用され、永続的なPATH変更は行われない。

## プロジェクト外へ書かれるもの

ローカルインストールでも、ダウンロードキャッシュとログは既存のnpmキャッシュへ書かれる。

```text
/home/mariko/.npm/
├─ _cacache/
├─ _logs/
└─ _npx/
```

これはnpmの既存標準配置であり、新しいキャッシュ置き場やPATHは作らない。不要になった場合も、他の
プロジェクトと共有しているため、このプロジェクトの判断だけで`~/.npm`全体を削除しない。

Firestore Emulator v1.21.0のJavaファイルは`~/.cache/firebase/emulators/`に約132 MiB保存する。
他プロジェクトも共有できるキャッシュのため、不要時には利用状況を確認してから個別ファイルを削除する。

## Security Rules試験用のJava 21

Firebase CLI 15.24.0の実行時要件に合わせ、Ubuntu公式の`openjdk-21-jre-headless`
21.0.11をOS共通基盤として`/usr/lib/jvm/java-21-openjdk-amd64/`へ追加した。導入後の
容量は約194 MiB。Java 17は削除せず、`/usr/bin/java`の既定はJava 17へ戻した。

`npm run test:rules`の実行中だけ`JAVA_HOME`と`PATH`の先頭をJava 21に指定する。永続的な
PATH変更は行わず、他のJava利用への影響を避ける。

## 中断されたインストールと再実行

2026-08-03の`npm install`は利用者によって中断された。確認時点では、次は存在しない。

- `node_modules/`
- `package-lock.json`
- 実行中のnpm installプロセス

その後、事前説明と利用者確認を経て`npm install`を再実行した。再実行は正常終了し、次を確認した。

- `node_modules/`：作成済み、約128 MiB
- `package-lock.json`：作成済み、約96 KiB
- グローバルnpmパッケージ：追加なし
- PATH：変更なし
- npmログ：終了コード0
- `npm run check`：成功
- `npm test`：1ファイル・1テスト成功
- `npm run build`：成功

## すでに追加したプロジェクトファイル

依存関係の実体ではなく、インストール計画と最小コードだけを追加済み。

- `package.json`
- `tsconfig.json`
- `vite.config.ts`
- `.gitignore`
- `.env.example`
- `index.html`
- `src/main.ts`
- `src/style.css`
- `src/smoke.test.ts`

## 現時点でインストールしないもの

- グローバルnpmパッケージ
- 新しいNode.js管理ツール
- Android SDK・Android Studio関連（既存SDKも今回のWeb方式では使用しない）
- Playwright本体や専用ブラウザ

`/home/mariko/.cache/ms-playwright`は既存しているが、このプロジェクトが作ったものではないため触らない。

## 後で必要性を判断するもの

### ブラウザ自動試験

初期試作はVitest、PCの既存Chrome、Android 16実機で確認する。Playwrightが本当に必要になった場合、
既存Chromeを使うか、専用ブラウザをダウンロードするか、配置・容量とともに事前相談する。

## 実行済みのインストール

プロジェクトディレクトリで通常の`npm install`を実行した。

- プロジェクト内：`node_modules/`と`package-lock.json`を作成済み
- ユーザー領域：既存の`~/.npm`キャッシュを利用
- グローバル：追加なし
- PATH：変更なし
- sudo：使用なし

2026-08-03にSecurity Rules試験のため、事前説明と利用者確認後に次を追加した。

- プロジェクト内：`firebase-tools` 15.24.0、`@firebase/rules-unit-testing` 5.0.1
- `node_modules/`：約265 MiBから約530 MiBへ増加
- ユーザーキャッシュ：Firestore Emulator v1.21.0を約132 MiB追加
- OS共通基盤：`openjdk-21-jre-headless` 21.0.11を約194 MiB追加
- 既定Java：導入時に21へ切り替わったため、利用者操作で17へ戻した
- 永続的なPATH変更、グローバルnpmパッケージ、Firebaseログイン：なし
- `npm run test:rules`：本番へ接続できないデモIDで12テスト成功

`npm audit`は開発依存のFirebase CLI配下にmoderate 5件を報告した。公開アプリに入る
本番依存は0件。npmが提示する自動修正はFirebase CLI 14系への破壊的なダウングレードのため、
現行版をローカルEmulatorに限定し、`npm audit fix --force`は実行しない。
