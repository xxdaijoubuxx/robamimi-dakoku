# ろばみみ打刻 要件

作成日：2026-08-03  
状態：合意済み  
対応WBS：3

この文書は、初版で満たす要件と対象外の正本とする。利用者から見た完成像は
`完成イメージ.md` を参照する。

## 1. 利用環境と前提条件

### 確定

- 利用者は一人である。
- 日常利用は利用者本人のスマートフォン一台を中心とする。
- スマートフォンのOSはAndroid 16である。
- 日常利用するブラウザはGoogle Chromeである。
- 日常の打刻はスマートフォンのホーム画面から開始する。
- ホーム画面の各入口は、Chromeの「ホーム画面に追加」で作るWebページショートカットとする。
- 追加時にホーム画面名を「起床」「就寝」「メモ」「履歴」へ手動設定する。
- Chromeの「アプリをインストール」で作るPWAは、日常打刻の入口として扱わない。
- スマートフォンの故障や機種変更で記録を失わないよう、端末外にも自動保存する。
- 本人確認やデータ保存にGoogleアカウントを利用してよい。
- 圏外や一時的な通信不良でも打刻を受け付け、端末内へ保持する。
- 圏外で保持した記録は、通信回復後に次回いずれかの入口を開いた時点で、自動的に端末外へ同期する。
- Chromeを閉じたまま、通信回復直後にバックグラウンド同期を完了することは初版の必須条件にしない。

### 未決（初版の利用可否には影響しない）

- 端末の機種

本人確認と端末外保存は、Firebase AuthenticationのGoogleログインとCloud Firestoreを採用した。
ネイティブアプリは初版で使用せず、通常ChromeのWebページショートカットを使う。

## 2. 機能要件

### 2.1 起床・就寝の打刻

- スマートフォンのホーム画面に「起床」と「就寝」の個別の入口を置く。
- それぞれの入口を1回タップすると、追加の確認や入力なしで記録を完了する。
- 記録日時には、利用者が入口をタップした時点の日時を用いる。
- オンライン時は、端末内と端末外への保存が完了したことを短く明確に表示する。
- 圏外や通信不良時も端末内への保存を完了し、「端末に保存済み・同期待ち」であることを表示する。
- 同期待ちの記録は、通信回復後の次回起動時に端末外へ自動同期する。
- 起床・就寝は一日に何回でも記録できる。
- 連続した同種の打刻を自動的に統合または削除せず、別々の記録として保持する。
- 連続打刻では重複の可能性を知らせてもよいが、確認操作は要求しない。
- 誤った記録は履歴から修正または削除できる。

### 2.2 メモ

- スマートフォンのホーム画面に「メモ」の個別の入口を置く。
- 「メモ」を1回タップした時点で、種類がメモの記録とその日時を保存する。
- タイムスタンプの保存後、任意の本文入力画面を開く。
- 本文は空でもよく、何も入力せず画面を閉じても最初の記録を保持する。
- 入力した本文は、明示的な保存ボタンを要求せず自動保存する。
- 圏外や通信不良時も、タイムスタンプと本文を端末内へ保存する。
- 同期待ちの記録と本文は、通信回復後の次回起動時に端末外へ自動同期する。
- 本文をあとから追記または修正しても、最初の打刻日時は変更しない。
- 端末内への保存済み、同期待ち、端末外への同期済みを利用者が区別できるようにする。

### 2.3 履歴

- 起床、就寝、メモを一つの時系列一覧で、新しい記録から表示する。
- 各記録に日時、種類、メモ本文を表示する。
- 同期待ちまたは同期失敗の記録では、その状態を表示する。
- 圏外でも、端末内に保存済みの履歴を閲覧できる。
- 記録の日時、種類、メモ本文をあとから修正できる。
- 圏外での修正は端末内へ保存し、通信回復後に端末外へ同期する。
- 削除は実行前に確認を求め、誤操作による削除を防ぐ。
- 初版では検索、絞り込み、集計、睡眠時間計算を提供しない。
- 通常の打刻では速さを優先し、例外的な修正と削除では安全性を優先する。

### 2.4 CSV出力

- 初版では期間指定を設けず、端末内にある全記録を一括出力する。
- 記録は新しいものから並べる。
- 修正済みの記録は最新の内容を出力する。
- 削除済みの記録は出力しない。
- 端末外へ未同期の記録も、端末内に保存されていれば出力に含める。
- 列は日時、種類、本文とする。
- 日時にはタイムゾーン情報を含める。
- 本文中のカンマ、改行、引用符を正しく扱うCSVとする。
- 日本語が文字化けしにくいUTF-8形式とする。
- ファイル名に出力日を含める。

### 2.5 本人確認、端末変更、データ復元

- 初回設定時にGoogleアカウントで本人確認する。
- 記録は本人確認に用いたGoogleアカウントへ結び付ける。
- 初回設定後の日常打刻では、ログイン操作を要求しない。
- URLを知っているだけの第三者には、記録、閲覧、変更を許可しない。
- 新しいスマートフォンでは、同じGoogleアカウントで本人確認すると同期済みの履歴を復元できる。
- 新しいスマートフォンで、ホーム画面の各入口を再設定できる。
- 認証が一時的に切れても、設定済み端末では打刻を端末内へ保存できる。
- 再認証が必要な状態を明示し、再認証後に同期待ちの記録を端末外へ同期する。
- 同期待ちの有無と最終同期時刻を確認できる。
- 通信できず端末内だけにある記録は、同期前に端末が故障すると復元できないことを既知の制約とする。
- 通信回復後は同期待ちの記録を速やかに自動同期し、未同期状態を見えるようにすることで消失リスクを小さくする。

### 2.6 例外処理

- 打刻時刻には、入口をタップした時点の端末時刻とタイムゾーンを保存する。
- 端末時計のずれなどで日時が誤った場合は、履歴から修正できる。
- 端末内への保存に失敗した場合は成功表示を出さず、失敗を明確に知らせる。
- 端末内への保存に成功し、端末外への同期だけ失敗した場合は同期待ちとして保持する。
- 同期は自動的に再試行し、必要な場合は利用者が手動でも再試行できる。
- 同じ記録の同期を再試行しても、端末外へ重複登録しない。
- 連続した打刻は別々の記録として保持し、重複の可能性だけを知らせる。
- メモ本文の自動保存に失敗した場合は、未保存であることを明示する。
- 同期失敗や再認証が必要な状態でも、端末内の既存記録を勝手に削除しない。
- 機種変更時などに同じ記録へ異なる変更が加えられた場合は、片方を黙って失わない。
- 自動回復できない問題では、失敗理由と利用者が取れる行動を表示する。
- 新版の必要ファイルを端末へ保存し終えるまでは、動作中の旧版を維持する。
- 新版の準備完了後は「更新する」を表示し、利用者の明示操作後に切り替えて再読み込みする。
- アプリ更新では、端末内の記録、同期状態、設定、診断ログを削除しない。

### 2.7 状態と診断

- 履歴から、最終同期、記録件数、同期待ち、同期失敗、再ログイン、競合を確認できる。
- 本文やGoogle認証情報を含まない診断情報を端末へ保存できる。
- 診断ログは端末内だけへ保存し、30日以内かつ最新500件へ自動整理する。
- 診断ログだけを確認後に消去でき、その操作で打刻記録を削除しない。

## 3. 初版の対象外

- 起床、就寝、メモ以外の項目追加
- 利用者が項目を作成・編集する設定画面
- 睡眠時間の自動計算
- 日次、週次、月次の集計やグラフ
- 履歴の検索、絞り込み、並べ替え
- リマインダーや通知
- センサーなどによる起床・就寝の自動判定
- 写真、音声、位置情報などの添付
- 既存データのCSVインポート
- LINEなど外部サービスとの連携
- PC向け画面
- 複数人での利用
- 複数端末からの日常的な同時利用
- 外観を細かく変更する設定

対象外の機能を将来追加することは禁止しない。初版を実際に利用し、必要性が確認できたものを
改修として検討する。

## 4. 受入条件

### 4.1 1タップ打刻

- Android 16のホーム画面から「起床」「就寝」を1回タップして記録できる。
- 追加の確認や入力を要求しない。
- タップ時の日時と種類が正しく保存される。
- 保存結果が明確に分かる。

### 4.2 メモ

- 「メモ」を1回タップした時点で、空本文の記録が残る。
- 本文を入力すると保存ボタンなしで自動保存される。
- 何も書かず閉じてもタイムスタンプが失われない。
- あとから本文を追記・修正できる。

### 4.3 圏外と同期

- 機内モードでも起床、就寝、メモを記録できる。
- 圏外中は同期待ちであることが分かる。
- 通信回復後、次回いずれかの入口を開くと自動同期される。
- 再試行しても記録が重複・欠落しない。

### 4.4 履歴と修正

- 全記録が新しい順の時系列一覧に表示される。
- 日時、種類、本文を修正できる。
- 確認後に削除できる。
- 圏外での修正も、通信回復後に同期される。

### 4.5 CSV

- 全件を新しい順で出力できる。
- 日時、種類、本文が正しい。
- 日本語、カンマ、引用符、改行を含むメモでも壊れない。
- 同期待ちの記録も端末内にあれば出力される。

### 4.6 本人確認と復元

- 第三者がURLを知っただけでは利用・閲覧できない。
- 同じGoogleアカウントを使い、新しい端末相当の環境へ同期済み履歴を復元できる。
- 再認証後に同期待ちの記録を同期できる。
- 同期失敗時に端末内の記録が消えない。

### 4.7 状態・診断・更新

- 状態画面で同期状態と件数を確認できる。
- 保存した診断情報に本文、メールアドレス、認証情報が含まれない。
- 新版へ明示的に更新した後も、端末内記録が維持される。

### 4.8 実利用

- 3日間、実際の起床、就寝、メモ記録に使用する。
- その間、原因不明の消失や重複がない。
- 起床直後と就寝直前でも、操作負担が許容できる。
- 問題があれば完成扱いにせず、要件、設計、実装の該当工程へ戻す。

3日間の試用は、長期的な習慣化ではなく、初版を完成扱いにできるかを判断するために行う。
