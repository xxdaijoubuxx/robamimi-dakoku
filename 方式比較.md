# 技術方式の比較

調査日：2026-08-03  
状態：採用方式合意済み  
対応WBS：4

## 採用方式

初版は、**GitHub Pagesで公開するChrome向けWebアプリ（PWA）**として作る。
Androidへの入口は、Chromeの「ホーム画面に追加」で作る四つのWebページショートカットとする。
「アプリをインストール」で作るインストール済みPWAは採用しない。

主な役割分担は次のとおり。

- GitHub：ソースコードの正本
- GitHub Pages：画面とプログラムの配信
- Chrome：ホーム画面への入口、画面表示、オフライン実行
- 端末内ブラウザ保存領域：圏外時を含む記録の正本
- Firebase Authentication：Googleアカウントによる本人確認
- Cloud Firestore：端末外保存と機種変更後の復元元

具体的なWeb技術、データ構造、同期方法、公開手順はWBS 5の設計で確定する。

## 訂正の履歴

当初はAndroid専用アプリを推奨した。PWAのManifest Shortcutが通常はアプリアイコンの
長押しメニューに表示されることから、独立したホーム画面アイコンには不向きと判断したためである。

当初は、Web App Manifestの異なる`id`と開始URLを使い、四つをインストール済みPWAとして導入する
方針を採用した。しかしAndroid 16実機では、`id`、開始URL、scope、Manifest URLを分離しても、二つ目の
就寝がインストール済みの起床PWAとして開かれ、四つを独立インストールできなかった。

一方、ChromeのWebページショートカットでは四つの独立アイコンを作成できた。記録後にURLを
`?done=1`へ切り替え、ホーム画面からの次回起動と結果画面の手動再読み込みを区別する設計に変更し、
GitHub Pages方式を継続する。これが実機で成立しない場合はAndroid専用アプリ案を再検討する。

参考：

- [Chrome: Web App ManifestのidによるPWAの識別](https://developer.chrome.com/docs/capabilities/pwa-manifest-id)
- [web.dev: Web App Manifest](https://web.dev/learn/pwa/web-app-manifest)

## 比較結果

| 観点 | GitHub Pages＋Chrome | Android専用アプリ |
|---|---|---|
| 導入 | Chromeから四つのWebショートカットを作り、短い名前を手動設定 | APKのビルドと導入が必要 |
| 更新 | GitHub Pagesの更新をChromeが取得 | APKを再ビルド・再導入 |
| 個別アイコン | 異なる開始URLのショートカットで実機作成済み | Pinned Shortcutで確実に対応可能 |
| 圏外保存 | ブラウザ内へ保存可能 | 端末DBへ保存可能 |
| 再同期 | 次回起動時の同期は可能。閉じたままの即時同期には制約 | WorkManagerでバックグラウンド実行しやすい |
| Google本人確認 | Firebase Authenticationで対応可能 | Firebase Authenticationで対応可能 |
| 開発・保守 | 比較的軽い | ビルド、署名、配布の保守が必要 |

今回の用途では、打刻のたびにWebアプリが起動するため、その時点で同期待ちデータを送る機会がある。
通信回復直後、アプリを一度も開かないまま同期を完了することまでは初版の必須条件にしない。

## ホーム画面の構成

同じWebアプリに、役割の異なる入口を用意する。

```text
起床ショートカット ─┐
就寝ショートカット ─┼─ 共通の記録処理・端末内データ
メモショートカット ─┤
履歴ショートカット ─┘
```

「起床」「就寝」「メモ」は、対応する開始URLを開いた時点で記録処理を始める。処理本体とデータは
複製せず、一つの正本を共有する。履歴は閲覧・修正・CSV出力の入口とする。

Chrome上で四つすべてを独立して追加できるか、追加後の表示名・アイコン・起動先が正しいかを、
本実装を広げる前に小さな試作で確認する。

## 圏外保存と同期

打刻では、最初に端末内のブラウザ保存領域へ一意なIDを持つ記録を保存する。端末内保存が成功した
時点で打刻成功とし、Firebaseへ送れていなければ「同期待ち」と表示する。

通信可能な起動時には同期待ちを送信する。同じIDを使って再試行し、二重登録を防ぐ。端末内データを
正本として、同期済み、同期待ち、失敗、競合を明示的に管理する。

Firestore自体にもWeb向けオフライン永続化があるが、同じ文書への複数変更はlast-write-winsとなる。
合意した「片方を黙って失わない」を守るため、Firestore任せにせずアプリ側で変更版と競合を管理する。

参考：

- [Firebase: Firestoreのオフライン利用](https://firebase.google.com/docs/firestore/manage-data/enable-offline)

## 本人確認とデータ保護

Firebase Authenticationを通じてGoogleアカウントで本人確認する。Firestoreでは利用者のUID配下に
記録を置き、Security Rulesで認証済みUIDが一致する場合だけ読み書きを許可する。

GitHub Pages上のJavaScriptやFirebase接続設定は公開されうるが、それだけで記録を読める状態には
しない。パスワードや秘密鍵はGitHubへ置かない。アクセス制御の正本はFirebase Authenticationと
Firestore Security Rulesに置く。

参考：

- [Firebase: GoogleアカウントによるWeb認証](https://firebase.google.com/docs/auth/web/google-signin)
- [Firebase: Firestore Security Rules](https://firebase.google.com/docs/firestore/security/rules-conditions)

## メモとCSV

メモの入口では、本文が空の記録を端末内へ保存してから入力画面を出す。本文は端末内へ自動保存し、
打刻日時とは別に更新日時と変更版を持つ。CSVは端末内の全記録から生成するため、圏外でも出力でき、
同期待ちの記録も含められる。

## 費用

GitHub Pagesは公開リポジトリで利用できる。FirebaseのSparkプランは支払情報なしで開始でき、Google
などのソーシャルログインは無償枠に含まれる。Firestoreの無料枠は1日あたり読み取り50,000件、
書き込み20,000件、保存1 GiBであり、個人の打刻用途は通常この範囲を大きく下回る。

無料枠を超えるとSparkプランでは該当サービスが停止しうるため、同期失敗は隠さず表示する。

参考：

- [GitHub Pagesについて](https://docs.github.com/pages/getting-started-with-github-pages/about-github-pages)
- [Firebaseの料金プラン](https://firebase.google.com/docs/projects/billing/firebase-pricing-plans)
- [Cloud Firestoreの無料枠](https://firebase.google.com/docs/firestore/pricing)

## 採用理由

- 利用者が理解しやすく、導入と更新が軽い
- GitHubをコードの正本にできる
- Chromeのホーム画面入口で日常操作を小さくできる可能性がある
- 圏外保存、Google本人確認、端末外保存をWeb技術で構成できる
- 実機で不足が判明した場合だけAndroid専用アプリへ移ればよい

## 既知のリスクと早期確認

- Android 16のChromeで四つのWebショートカットを独立アイコンとして追加できるか
- 記録後のURL切替により、次回タップと手動再読み込みを区別できるか
- 1タップ後、余計な確認なく記録処理が始まるか
- 圏外起動時にもアプリ本体が読み込まれ、端末内へ保存できるか
- Chromeを閉じた状態からの起動でGoogle認証状態と端末内データを利用できるか
- 通信回復後の次回起動で同期待ちが欠落・重複なく送られるか

これらは設計後の最小試作で先に確認し、成立しない場合は方式選定へ戻る。
